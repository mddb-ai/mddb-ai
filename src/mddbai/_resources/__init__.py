"""Static resources inside the mddbai package (skills / hooks).

`mddbai init --with-claude-hook` extracts the SKILL.md / hook .py files
from this folder into the user project's ``.claude/{skills,hooks}/``.
"""
